# FCC 5G Coverage-Change Audit Pipeline - pinned dependencies
#
# Supports Python 3.9 (RHEL 9 stock) through 3.14 via PEP 508 markers.
# Same command everywhere:
#     pip install -r requirements.txt
# On RHEL 9 / Python 3.9 prefer the locked tree for a reproducible install:
#     pip install -r requirements-py39.lock.txt
#
# Modern pins (3.11+/3.12+) match the verified macOS/Windows overnight path.
# Older pins are the last PyPI releases that still publish cp39 wheels.

# --- core data / geospatial ---
numpy==2.5.0; python_version >= "3.12"
numpy==2.4.6; python_version == "3.11"
numpy==2.0.2; python_version < "3.11"

pandas==3.0.3; python_version >= "3.11"
pandas==2.3.3; python_version < "3.11"

shapely==2.1.2; python_version >= "3.10"
shapely==2.0.7; python_version < "3.10"

pyproj==3.7.2; python_version >= "3.10"
pyproj==3.6.1; python_version < "3.10"

geopandas==1.1.3; python_version >= "3.10"
geopandas==1.0.1; python_version < "3.10"

pyogrio==0.12.1; python_version >= "3.10"
pyogrio==0.11.1; python_version < "3.10"

matplotlib==3.10.8; python_version >= "3.10"
matplotlib==3.9.4; python_version < "3.10"

contextily==1.6.2          # topographic basemap tiles for compare maps
xyzservices==2025.4.0      # tile provider registry (contextily dep)

h3==4.5.0; python_version >= "3.10"
h3==4.4.2; python_version < "3.10"

duckdb==1.5.4; python_version >= "3.10"
duckdb==1.4.5; python_version < "3.10"

pyarrow==24.0.0; python_version >= "3.10"
pyarrow==21.0.0; python_version < "3.10"

# --- machine learning (all local; no external API) ---
scikit-learn==1.9.0; python_version >= "3.11"
scikit-learn==1.6.1; python_version < "3.11"

scipy==1.18.0; python_version >= "3.12"
scipy==1.17.1; python_version == "3.11"
scipy==1.13.1; python_version < "3.11"

# --- HTTP / config ---
requests==2.34.2; python_version >= "3.10"
requests==2.32.5; python_version < "3.10"

PyYAML==6.0.3
certifi==2026.6.17

# On 3.9, redshift-connector/botocore resolve to urllib3 1.26.x; 2.x is fine on 3.10+.
urllib3==2.7.0; python_version >= "3.10"
urllib3==1.26.20; python_version < "3.10"

# Pin modern boto on 3.9 so pip does not backtrack into ancient botocore 1.12.x.
botocore>=1.34,<2; python_version < "3.10"
boto3>=1.34,<2; python_version < "3.10"

# --- optional: rendered-tile reconciliation + CV fallback ---
# The vector pipeline runs without these; they are only used by reconcile.py.
rasterio==1.5.0; python_version >= "3.10"
rasterio==1.4.3; python_version < "3.10"

Pillow==12.2.0; python_version >= "3.10"
Pillow==11.3.0; python_version < "3.10"

scikit-image==0.26.0; python_version >= "3.10"
scikit-image==0.24.0; python_version < "3.10"

mercantile==1.2.1

# --- Redshift backend (work laptop / warehouse path) ---
redshift-connector==2.1.5

# --- SHAP feature explanations (optional; used by validate.py when available) ---
# Commented out by default: shap pulls in numba/llvmlite with a narrow Python
# range. validate.py imports shap in a try/except and degrades gracefully.
# To enable SHAP, use a Python 3.11–3.13 venv and `pip install shap`.
# shap==0.47.2

# --- dev / testing (optional) ---
pytest==8.4.2
pytest-timeout==2.4.0
pytest-cov==6.2.1
hypothesis==6.136.2
ruff==0.12.0
mypy==1.16.1
