# FCC 5G Coverage-Change Audit Pipeline - pinned dependencies
# Verified to install on Python 3.13 and 3.14 (macOS arm64).
# If your work laptop has a different Python, these minimum versions still apply;
# pip will pick compatible wheels.

# --- core data / geospatial ---
numpy==2.5.0
pandas==3.0.3
shapely==2.1.2
pyproj==3.7.2
geopandas==1.1.3
pyogrio==0.12.1          # fast vector I/O backend for geopandas
h3==4.5.0                # Uber H3 hex indexing (matches FCC audit geography)
duckdb==1.5.4            # out-of-core joins for nationwide data
pyarrow==24.0.0          # parquet engine for interim caching

# --- machine learning (all local; no external API) ---
scikit-learn==1.9.0      # HDBSCAN (site inference) + IsolationForest (anomaly ranking)
scipy==1.18.0

# --- HTTP / config ---
requests==2.34.2
PyYAML==6.0.3
certifi==2026.6.17
urllib3==2.7.0

# --- optional: rendered-tile reconciliation + CV fallback ---
# The vector pipeline runs without these; they are only used by reconcile.py.
rasterio==1.5.0
Pillow==12.2.0
scikit-image==0.26.0
mercantile==1.2.1

# --- Redshift backend (work laptop / warehouse path) ---
redshift-connector==2.1.5

# --- dev / testing (optional) ---
pytest==8.4.2
