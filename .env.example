# Local secrets / environment overrides (git-ignored). Copy to ".env":
#     Windows:  copy .env.example .env
#     mac/Linux: cp .env.example .env
#
# NOTE: FCC downloads need NO API token — the pipeline uses the same public
# endpoint the broadbandmap.fcc.gov "Download" buttons call. This file is only
# needed if you switch source.backend to "redshift".

# Redshift (copy connection details from DBeaver → your connection → Edit).
# Only needed when source.backend is "redshift" (opt-in). The public / file-only
# path uses source.backend: files and reads cached parquet under data/raw/.
#
# IMPORTANT: schema bdc_dataplatform lives in db_fcc_bdc (not db_fcc_bdp_ext).
# REDSHIFT_HOST=your-cluster.xxxxx.us-east-1.redshift.amazonaws.com
# REDSHIFT_DB=db_fcc_bdc
# REDSHIFT_USER=your_username
# REDSHIFT_PASSWORD=your_password
#
# Then: python -m fcc_audit.cli --backend redshift doctor
# The backend reads the pre-aggregated H3 res-9 hex tables
# (<schema>.bbmap_mobile_bb_tech_hex9s_<build>); configure source.redshift.schema /
# hex_table_prefix / environment / service_hex_columns, and set analysis.vintages
# to two build ids (the numeric table-name suffix), e.g. current: "277", prior: "279".
